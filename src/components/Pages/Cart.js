import Cart from "../Cart/Cart";

function Carts() {
    return (
        <div className="Products">
            {/* <button onClick={clearCart}>Limpiar Carrito</button>
            {cart.map((item, index) => (
            <Cart key={index} data={item} delFromCart={delFromCart}/>
            ))}*/}
        </div>
    );
}

export default Carts;
